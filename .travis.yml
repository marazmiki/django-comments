language: python

python:
#  - "2.6"
  - "2.7"
#  - "3.2"
#  - "3.3"
  - "3.4"

env:
  - DJANGO=1.5.8
  - DJANGO=1.6.5

install:
  - pip install -q Django==$DJANGO
  - pip install -q flake8
  - python setup.py install

before_script:
  - flake8 --exclude=migrations --ignore=W391,E501,E225 django_comments

script:
  - python tests.py